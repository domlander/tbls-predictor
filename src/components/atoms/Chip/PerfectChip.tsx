import React from "react";
import colours from "../../../styles/colours";
import Chip from "./Chip";

const PerfectChip = () => (
  <Chip
    label="PERFECT"
    colour={colours.grey100}
    backgroundColour={colours.gold300}
  />
);

export default PerfectChip;
